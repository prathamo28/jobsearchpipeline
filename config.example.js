// Copy to config.js and add your Supabase keys (or leave empty to use only localStorage).
// On Vercel: set SUPABASE_URL and SUPABASE_ANON in env; build creates config.js.
window.SUPABASE_URL = '';
window.SUPABASE_ANON = '';
